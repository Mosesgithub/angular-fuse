<StackPanel ux:Class="MyApp" [Width]="amount" [Background]="background" Height="300" (Clicked)="clickHandler(amount)">
    <Text [Value]="textvalue"></Text>
    <StackPanel *ngFor="#f in foo">
        <Text [Value]="f.background" />
        <MyCircle />
    </StackPanel>
</StackPanel>
